const app = require('./lib/app');
const mongoose = require('mongoose');

// mongoose.connect('mongodb://localhost:27017/human_habits');
mongoose.connect();

const PORT = ;

app.listen(PORT), () => {

}